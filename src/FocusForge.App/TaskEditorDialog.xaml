<Window x:Class="FocusForge.App.TaskEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Add task" Width="480" Height="700"
        WindowStartupLocation="CenterOwner" ResizeMode="CanMinimize"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource InkBrush}"
        FontFamily="Times New Roman">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Simple modern TextBox style -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Margin" Value="0,0,0,16" />
            <Setter Property="Background" Value="{DynamicResource PanelBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource InkBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource GraphGridBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern flat ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,0,0,16" />
            <Setter Property="Background" Value="{DynamicResource PanelBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource InkBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource GraphGridBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="30" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Content="{TemplateBinding SelectionBoxItem}" 
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" 
                                                      Margin="{TemplateBinding Padding}" 
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                      IsHitTestVisible="False" />
                                    <ToggleButton Grid.Column="1" Focusable="False" IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent">
                                                    <Path Data="M 0 0 L 4 4 L 8 0 Z" Fill="{DynamicResource MutedBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Border>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource GraphGridBrush}" BorderThickness="1" CornerRadius="6" Margin="0,4,0,0">
                                    <ScrollViewer Margin="0,4">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern flat DatePicker -->
        <Style TargetType="DatePicker">
            <Setter Property="Margin" Value="0,0,0,16" />
            <Setter Property="Background" Value="{DynamicResource PanelBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource InkBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource GraphGridBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="30" />
                                    </Grid.ColumnDefinitions>
                                    <DatePickerTextBox x:Name="PART_TextBox" 
                                                       Grid.Column="0" 
                                                       Focusable="{TemplateBinding Focusable}" 
                                                       HorizontalContentAlignment="Stretch" 
                                                       VerticalContentAlignment="Stretch" 
                                                       Margin="{TemplateBinding Padding}" 
                                                       Background="Transparent" 
                                                       BorderThickness="0" 
                                                       Foreground="{TemplateBinding Foreground}" />
                                    <Button x:Name="PART_Button" Grid.Column="1" Focusable="False" Padding="0">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="Transparent">
                                                    <Path Data="M 2 2 L 14 2 L 14 14 L 2 14 Z M 4 0 L 4 4 M 12 0 L 12 4 M 2 6 L 14 6" Stroke="{DynamicResource MutedBrush}" StrokeThickness="1.5" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                    <Popup x:Name="PART_Popup" AllowsTransparency="True" Placement="Bottom" PlacementTarget="{Binding ElementName=PART_TextBox}" StaysOpen="False" />
                                </Grid>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>
        </ResourceDictionary>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32">
            <TextBlock x:Name="HeadingText" Text="Add task" FontSize="26" FontWeight="Bold" Margin="0,0,0,24" />
            
            <TextBlock x:Name="TitleLabelText" Text="Task name" Style="{StaticResource LabelStyle}" />
            <TextBox x:Name="TitleInput" />
            
            <TextBlock x:Name="DetailLabelText" Text="Details" Style="{StaticResource LabelStyle}" />
            <TextBox x:Name="DetailInput" AcceptsReturn="True" MinHeight="60" TextWrapping="Wrap" />
            
            <TextBlock x:Name="TimeLabelText" Text="Time, for example 3:00 PM" Style="{StaticResource LabelStyle}" />
            <materialDesign:TimePicker x:Name="TimeInput" Is24Hours="False" Padding="10,8" Margin="0,0,0,16" Background="{DynamicResource PanelBrush}" Foreground="{DynamicResource InkBrush}" BorderBrush="{DynamicResource GraphGridBrush}" BorderThickness="1" />
            
            <TextBlock Text="Schedule" Style="{StaticResource LabelStyle}" />
            <ComboBox x:Name="ScheduleInput" SelectionChanged="ScheduleInput_SelectionChanged" />
            
            <DatePicker x:Name="DueDateInput" SelectedDateChanged="DueDateInput_SelectedDateChanged" />
            
            <TextBlock Text="Repeat" Style="{StaticResource LabelStyle}" />
            <ComboBox x:Name="RecurrenceInput" />
            
            <TextBlock Text="Tags, separated by commas" Style="{StaticResource LabelStyle}" />
            <TextBox x:Name="TagsInput" />
            
            <TextBlock Text="Checklist items" Style="{StaticResource LabelStyle}" />
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="ChecklistInput" Margin="0,0,8,0" Grid.Column="0" />
                <Button Content="Add" Style="{DynamicResource GhostButton}" Padding="16,8" Grid.Column="1" Click="AddChecklistItem_Click" VerticalAlignment="Top" Height="34" />
            </Grid>
            
            <ListBox x:Name="ChecklistItems" MaxHeight="120" Margin="0,0,0,12" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource GraphGridBrush}" />
            
            <Button Content="Remove selected" Style="{DynamicResource GhostButton}" HorizontalAlignment="Left" Padding="12,6" Click="RemoveChecklistItem_Click" />
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,32,0,0">
                <Button Content="Cancel" Style="{DynamicResource GhostButton}" Padding="20,10" Margin="0,0,12,0" Click="Cancel_Click" />
                <Button Content="Save" Style="{DynamicResource PrimaryButton}" Padding="24,10" Click="Save_Click" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Window>
